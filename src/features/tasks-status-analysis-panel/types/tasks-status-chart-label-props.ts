import { MUIStyledCommonProps, Theme } from "@mui/system";
import { CSSProperties } from "react";

export interface TasksStatusChartLabelProps extends MUIStyledCommonProps<Theme> {
  customStyles: CSSProperties;
}